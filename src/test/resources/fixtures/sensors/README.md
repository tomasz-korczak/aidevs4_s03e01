# Sensor fixtures

Sample JSON files for local validation live in this directory.

Set `JSON_DIR` to this folder and run the packaged jar with no CLI arguments.

See `specs/001-corrupt-json-report/quickstart.md`.
